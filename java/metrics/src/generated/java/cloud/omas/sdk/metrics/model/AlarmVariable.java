/**
 * Copyright (c) 2026 Omas Cloud
 *
 * SPDX-License-Identifier: MIT
 *
 * Code generated by the Omas Java SDK generator. DO NOT EDIT.
 */

package cloud.omas.sdk.metrics.model;

import cloud.omas.sdk.metrics.model.Dimension;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonTypeName;
import com.fasterxml.jackson.annotation.JsonValue;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;
import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonValue;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.fasterxml.jackson.databind.annotation.JsonPOJOBuilder;

import java.math.BigDecimal;
import java.util.List;
import java.util.Map;
import java.util.Objects;
import java.util.Set;

/**
 * AlarmVariable data model.
 */
@JsonDeserialize(builder = AlarmVariable.Builder.class)
public final class AlarmVariable {

    private final String name;
    private final String metricName;
    private final List<Dimension> includeDimensions;
    private final List<Dimension> excludeDimensions;
    private final String aggregation;

    private AlarmVariable(Builder builder) {
        Objects.requireNonNull(builder.name, "name");
        if (builder.name != null && builder.name.toString().length() > 64) {
            throw new IllegalArgumentException("name is too long");
        }
        if (builder.name != null && !builder.name.toString().matches("^[A-Za-z_][A-Za-z0-9_]*$")) {
            throw new IllegalArgumentException("name has an invalid format");
        }
        this.name = builder.name;
        Objects.requireNonNull(builder.metricName, "metricName");
        if (builder.metricName != null && builder.metricName.toString().length() > 255) {
            throw new IllegalArgumentException("metricName is too long");
        }
        if (builder.metricName != null && !builder.metricName.toString().matches("^[A-Za-z0-9_.-]+$")) {
            throw new IllegalArgumentException("metricName has an invalid format");
        }
        this.metricName = builder.metricName;
        if (builder.includeDimensions != null && builder.includeDimensions.size() > 30) {
            throw new IllegalArgumentException("includeDimensions has too many items");
        }
        this.includeDimensions = builder.includeDimensions == null ? null : List.copyOf(builder.includeDimensions);
        if (builder.excludeDimensions != null && builder.excludeDimensions.size() > 30) {
            throw new IllegalArgumentException("excludeDimensions has too many items");
        }
        this.excludeDimensions = builder.excludeDimensions == null ? null : List.copyOf(builder.excludeDimensions);
        Objects.requireNonNull(builder.aggregation, "aggregation");
        if (builder.aggregation != null && builder.aggregation.toString().length() < 3) {
            throw new IllegalArgumentException("aggregation is too short");
        }
        if (builder.aggregation != null && builder.aggregation.toString().length() > 128) {
            throw new IllegalArgumentException("aggregation is too long");
        }
        this.aggregation = builder.aggregation;
    }

    /**
     * Creates a builder for this model.
     *
     * @return a new builder
     */
    public static Builder builder() {
        return new Builder();
    }

    /**
     * Expression variable name. Must start with a letter or underscore and may contain only letters, numbers, and underscores.
     *
     * @return the name value
     */
    @JsonProperty("name")
    public String name() {
        return name;
    }

    /**
     * Metric name using only letters, numbers, dots, underscores, and hyphens.
     *
     * @return the metricName value
     */
    @JsonProperty("metricName")
    public String metricName() {
        return metricName;
    }

    /**
     * Dimension filters to include for the metric query.
     *
     * @return the includeDimensions value
     */
    @JsonProperty("includeDimensions")
    public List<Dimension> includeDimensions() {
        return includeDimensions;
    }

    /**
     * Dimension filters to exclude for the metric query.
     *
     * @return the excludeDimensions value
     */
    @JsonProperty("excludeDimensions")
    public List<Dimension> excludeDimensions() {
        return excludeDimensions;
    }

    /**
     * Aggregation expression to apply to metric data. Supported forms are sum, max, min, avg, count, percentile(&lt;0-100&gt;), trimmed_mean(&lt;lower&gt;;&lt;upper&gt;), trimmed_mean(&lt;lower&gt;;), and trimmed_mean(;&lt;upper&gt;). Trimmed-mean bounds are percentages from 0 to 100, and at least one bound must be present.
     *
     * @return the aggregation value
     */
    @JsonProperty("aggregation")
    public String aggregation() {
        return aggregation;
    }

    /**
     * Builds an immutable {@link AlarmVariable}.
     */
    @JsonPOJOBuilder(withPrefix = "")
    public static final class Builder {

        private String name;
        private String metricName;
        private List<Dimension> includeDimensions;
        private List<Dimension> excludeDimensions;
        private String aggregation;

        private Builder() {}

        /**
         * Sets name.
         *
         * @param name Expression variable name. Must start with a letter or underscore and may contain only letters, numbers, and underscores.
         * @return this builder
         */
        public Builder name(String name) {
            this.name = name;
            return this;
        }

        /**
         * Sets metricName.
         *
         * @param metricName Metric name using only letters, numbers, dots, underscores, and hyphens.
         * @return this builder
         */
        public Builder metricName(String metricName) {
            this.metricName = metricName;
            return this;
        }

        /**
         * Sets includeDimensions.
         *
         * @param includeDimensions Dimension filters to include for the metric query.
         * @return this builder
         */
        public Builder includeDimensions(List<Dimension> includeDimensions) {
            this.includeDimensions = includeDimensions;
            return this;
        }

        /**
         * Sets excludeDimensions.
         *
         * @param excludeDimensions Dimension filters to exclude for the metric query.
         * @return this builder
         */
        public Builder excludeDimensions(List<Dimension> excludeDimensions) {
            this.excludeDimensions = excludeDimensions;
            return this;
        }

        /**
         * Sets aggregation.
         *
         * @param aggregation Aggregation expression to apply to metric data. Supported forms are sum, max, min, avg, count, percentile(&lt;0-100&gt;), trimmed_mean(&lt;lower&gt;;&lt;upper&gt;), trimmed_mean(&lt;lower&gt;;), and trimmed_mean(;&lt;upper&gt;). Trimmed-mean bounds are percentages from 0 to 100, and at least one bound must be present.
         * @return this builder
         */
        public Builder aggregation(String aggregation) {
            this.aggregation = aggregation;
            return this;
        }

        /**
         * Builds the immutable model.
         *
         * @return the built model
         */
        public AlarmVariable build() {
            return new AlarmVariable(this);
        }
    }
}
